langcode: da
status: true
dependencies:
  config:
    - user.role.kommunikation
  module:
    - user
id: user_remove_role_action.kommunikation
label: 'Remove the Kommunikation role from the selected user(s)'
type: user
plugin: user_remove_role_action
configuration:
  rid: kommunikation
